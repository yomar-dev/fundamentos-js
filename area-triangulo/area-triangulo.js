let base = 6
let altura = 9

function areaTriangulo(base, altura){
	return (base * altura) / 2
}

console.log(`El área del triángulo de base ${base} y altura ${altura} es: ${areaTriangulo(base, altura)}`)